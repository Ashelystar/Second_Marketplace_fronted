# 后端根地址（协议 + 主机 + 端口），不要写路径里的 /api
# npm run dev 时，Vite 会把浏览器里的 /api/* 代理到该地址
#
# 示例：本机后端
# VITE_API_BASE_URL=http://localhost:8080
#
# 示例：前后端同 IP，前端 5173、后端 8083
# VITE_API_BASE_URL=http://1.117.73.156:8083
#
VITE_API_BASE_URL=http://localhost:8080
